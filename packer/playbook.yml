
- hosts:
    - all
  remote_user: ubuntu
  become: yes

  gather_facts: no

  pre_tasks:
    - name: Update cache
      raw: yum repolist

  roles:
    - initOS
    - amznCodeDeployAgent
    - amznSSMAgent
    - systemUtils
    - hardenSSHD
    - cleanupOS

